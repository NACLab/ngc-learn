{
  title: "Steps",
  description: "A list of all steps of the cycle",
  type: "array",
  items: {
    description: "The command name being run",
    type: "object",
    required: ["command_name"],
    properties: {
      command_name: {
        type: "string"
      }
    }
  }
}
